name: howe_lab_to_nwb_env
channels:
- conda-forge
- defaults
dependencies:
- python>=3.9
- bioformats_jar  # required for BioFormats imaging extractor
- pip
- pip:
  - -e .  # This calls the setup and therefore requirements minimal
